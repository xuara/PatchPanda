name: Build and publish unstable Docker image

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to deploy"
        required: true
        default: "main"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: docker-unstable-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write # Required for security attestations (v7)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref }}

      - name: Lowercase repo name
        run: echo "REPO_LC=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          file: ./Dockerfile
          push: true
          build-args: |
            RELEASE_VERSION=unstable-${{ github.sha }}
          tags: ghcr.io/${{ env.REPO_LC }}:unstable
          provenance: true
          sbom: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

  test-multi-arch:
    needs: build-and-push
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false # Run all architectures even if one fails
      matrix:
        arch: [amd64, arm64, arm/v7]
    steps:
      - name: Lowercase repo name
        run: echo "REPO_LC=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Test image on ${{ matrix.arch }}
        run: |
          set -e
          echo "Starting smoke test for ${{ matrix.arch }}..."
          # We use a 3-minute timeout to allow slow arm/v7 emulation to boot fully.
          # Exit code 124 means the timeout hit, which confirms the app stayed up.
          timeout 180s docker run --rm --platform linux/${{ matrix.arch }} \
            ghcr.io/${{ env.REPO_LC }}:unstable || [ $? -eq 124 ]
          echo "Successfully verified image execution for ${{ matrix.arch }}"
